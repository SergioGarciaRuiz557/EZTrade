package com.trading.platform.eztrade.portfolio.application.ports.out;

/**
 * Output port for publishing events generated by portfolio.
 * <p>
 * Prevents the application layer from depending directly on Spring Events.
 */
public interface DomainEventPublisherPort {

    /** Publishes a domain or integration event inside the Spring process. */
    void publish(Object event);
}

